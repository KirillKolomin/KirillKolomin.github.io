export async function getBalanceOfUserFromDatabase(userId: string): Promise<number> {
  // Imagine we use userId to get balance from database
  // const balance = fireBase.getBalance(userId); // or something like that
  console.log(userId);

  return 2500;
}
